import { ExtensionType } from "interfaces/content/Article"

interface DataPrevisualizerInterface {
  uri: string
  name: string
  extension: string
  type: ExtensionType
}

export default DataPrevisualizerInterface
